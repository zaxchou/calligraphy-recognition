# 写意知识库工作日志 · 2026-04-29 ~ 2026-04-30

> 自动记录规则：每日工作结束后生成 `summary_日期.md`，记录当天所有改动与决策。

---

## 4月29日（修复日）

### 搜索配图修复
- 配图 404：前端 `getImageUrl` 增加 UUID 三段式 fallback（`stored_url → url → id`），后端新增 `GET /images/{id}` 端点
- 缩略图扁形：全站 `object-fit: cover` → `contain` + `aspect-ratio: 1`（正方形）
- 关联配图为空：后端 `associated_images` 同时返回 `url` 和 `stored_url`，不足时从邻近 ±3 chunk 补充

### $...$ LaTeX 残留彻底修复
- 根因：MinerU PDF 解析保留数学公式标记
- 修复：`mineru_parser.py` 入口清洗 + `knowledge_api.py` 输出清洗（`_STRIP_LATEX_RE`）+ 前端 `cleanLatex()` 三层防御
- 被遗漏的输出路径（`context_before`/`context_after`/`markdown`）已补齐

### 搜索分数修复
- 所有结果显示 2%：使用的是原始 Qdrant cosine 分数（0.01~0.02）
- 修复：输出改用 `rerank_score`，capped at 100%

### 图片预览翻页
- 新增上一张/下一张按钮 + 键盘 ArrowLeft/ArrowRight + Escape 关闭 + 计数器

### BM25 持久化 + 任务恢复
- `hybrid_search.py`：BM25 索引 pickle 持久化到磁盘
- `database.py`：启动时 `recover_stalled_tasks()` 恢复中断任务

---

## 4月30日（优化日）

### 上午：UI/UX 大改版（KnowledgeSearch.vue）

#### 动画升级
- 三个独立 `<transition>` 合并为单一 `mode="out-in"`，旧视图完全退出后新视图进入（解决重叠闪烁）
- 新增 `view-switch` 动画：320ms `cubic-bezier(0.4,0,0.2,1)`（Material Standard 缓动）
- 搜索结果卡片：staggered 入场（`animation-delay: i × 0.06s`）
- 搜索框聚焦：光晕呼吸脉动 `@keyframes ks-glow-pulse`（2s 周期）
- 进度条：渐变 shimmer 流动效果

#### 功能优化
- 删除「图谱」功能（SVG 知识关系图 + graphNodes/Edges/Gesture handlers 全部移除）
- 「搜索/专家」改为「搜索模式/专家模式」
- 模式按钮 vs 搜索历史标签视觉分离：按钮加粗边框+投影，标签无边框+浅灰底
- 顶栏搜索框输入不再跳回居中态（移除 `@input="centered=true"`）
- 居中态点击书库面板不再改变搜索框位置（移除 `toggleLib` 中的 centered 设置）
- 书库面板删除搜索历史和搜索范围区块

#### 后端去重
- `knowledge_api.py`：输出前新增内容指纹去重（`book_id + content[:80]`），消除多个改写 query + 跨模态搜索产生的重复结果

#### 参考来源可点击
- `scrollToResult()` 从严格 `===` 匹配改为三级模糊匹配：书名模糊匹配 → 页码 ±2 容差 → 打开详情面板

### 下午：Composition 模块两轮优化

#### 第一轮：知识库注入（`13dd078`）
- `search_cases()` 删除 `source=uploaded_images` 过滤，扩展搜索 `bird_flower_tutorial` 等全集合
- `_fetch_knowledge_context()`：搜索 `knowledge_texts` 原文注入 LLM prompt（~60 行）
- `_build_score_table()` 扩展：每维度附带 120 字系统分析 + 100 字建议
- `_safe_knowledge()` 格式化函数（2000 字截断，5 段原文）

#### 第二轮：插图修复 + 起承转合改版（`6e45c0b`）
- Qdrant 新增 `scroll_by_filter()` + `figure_image_url_from_qdrant()` 按 figure_id 补图
- LLM prompt 追加要求 #9：结语后输出起承转合坐标 JSON
- 新增 `extract_qczh_coords()` 从 LLM 文本提取坐标
- `analyze_arrow_flow()` 改为空操作，`draw_qczh_from_llm()` 从坐标画箭头
- Pipeline 重排：`llm_narrative` → `arrow_analysis`（arrow_analysis 移到 LLM 之后）

### 文件清理
- 删除 2 个孤立图片目录（0.23GB，无 DB 记录的 `08ddd32f` + `2338dfa1`）
- 删除上传目录残留 549 个文件（0.55GB：534 JPG + 7 PNG + 7 PDF + 1 ZIP）
- 删除临时文档/脚本（`build_err.txt`, `build_out.txt`, `check_pdfjs.mjs`, `progress.md`, `_dg.py`）
- 用户从回收站恢复了题跋分析上传文件（存在 65 张误差但 DB 记录全部完整）

### 工作记录规范
- Plan 文件：`reports/plan_*.md`
- 工作总结：`reports/summary_日期_主题.md`
- 日报：`reports/summary_日期.md`
- 核心记忆：关键改动记录到 project memory

---

## 文件改动统计

| 日期 | 核心文件 | 新增/修改 |
|------|---------|----------|
| 4/29 | `knowledge_api.py`, `KnowledgeSearch.vue`, `mineru_parser.py`, `hybrid_search.py`, `reranker.py`, `BookReaderModal.vue`, `SearchResultDetail.vue` | 多个修复 |
| 4/30 | `KnowledgeSearch.vue`, `composition_llm.py`, `stages.py`, `tasks.py`, `qdrant_client.py`, `figure_assets.py`, `knowledge_api.py` | ~300 行净增 |

## 提交清单

```
6e45c0b Composition: 插图修复+起承转合改版
13dd078 Composition优化: 知识库原文注入LLM+图片搜索扩展+维度分析文本增强
d7f5ba9 切换动画重构: 三视图合并为单一transition mode=out-in 320ms串行过渡
b9f6699 UI优化: 去重+动画升级+书库精简+来源可点击
2ce15f1 feat: 图片预览翻页+键盘支持+计数器
54fac18 fix: 搜索分数改用rerank_score+邻近chunk丰富配图
62e6b7d fix: 缩略图三级降级+@error回退
599d0ba fix: 系统性修复缩略图比例
3bc3123 fix: cleanLatex三层防御
3fc5d43 fix: UUID图片直达+前端防御
ed00406 fix: $残留全面清除+BookReaderModal修url
eb164fa fix: 系统性修复$+BM25持久化+任务恢复+多轮对话+大纲
```

## 待办

- [ ] Git GC 回收历史大文件（calligraphy.db 多版本、PDF 二进制等，~0.3GB）
- [ ] 重启后端使 composition 优化生效
